---
deprecations:
  - nova::compute::vnc_keymap option is now deprecated for removal, the
    parameter has no effect.
  - nova::compute::spice::keymap option is now deprecated for removal,
    the parameter has no effect.

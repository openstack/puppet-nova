---
deprecations:
  - |
    The following parameters of the ``nova::scheduler::filters`` class have
    been deprecated.

    - ``aggregate_image_properties_isolation_namespace``
    - ``aggregate_image_properties_isolation_separator``
